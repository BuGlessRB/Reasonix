{
  "name": "reasonix-studio-electron",
  "version": "0.0.0",
  "private": true,
  "description": "Electron shell for Reasonix Studio over the loopback host",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build:host": "go build -o bin/ ../../cmd/reasonix-studio-host",
    "test": "node --test test/unit.mjs",
    "build:app": "electron-builder --dir",
    "build:dist": "electron-builder",
    "smoke": "electron test/smoke.js"
  },
  "devDependencies": {
    "electron": "44.1.0",
    "electron-builder": "^26.15.3",
    "playwright": "1.62.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  }
}
